/* #183: Subject and its algebra retired. All identity flows through
   `trace::SelectorVariant` (see trace-types.hh) and
   `TracingDecisionGraph::computeSelectorHash`.

   This TU is empty; kept in the build to preserve the module's spot
   until the file is renamed. */

#include "nix/expr/subject-id.hh"
